#!/bin/bash

CURRENT_DIR=$(pwd)/$(dirname $0)/;
source $CURRENT_DIR/functions

TELELAB_PATH=$(dirname CURRENT_DIR)/;
VENDOR_PATH=$TELELAB_PATH"vendor";

# Need subversion
install_package subversion
install_package git

create_dir $VENDOR_PATH;
cd $VENDOR_PATH;

# Install smarty 3
show_action "INSTALL OR UPDATE SMARTY";
svn checkout http://smarty-php.googlecode.com/svn/trunk/distribution smarty


# Install PHPMAiler
show_action "INSTALL OR UPDATE PHPMailer";
svn checkout http://svn.codespot.com/a/apache-extras.org/phpmailer/trunk/ phpmailer

# Install Atoum
show_action "INSTALL OR UPDATE Atoum";
create_dir atoum
cd atoum
wget http://downloads.atoum.org/nightly/mageekguy.atoum.phar
cd -

# Install ApiGen
create_dir apigen
cd apigen
git init
git pull https://github.com/apigen/apigen.git
cd -

# Install ExcelReader
create_dir phpexcel
cd phpexcel
git init
git pull https://github.com/PHPOffice/PHPExcel.git
cd -