; Configuration file of Test
[logger]
; engine = file
; enable = true
; log_level = DEBUG
; file[file] = ROOT_DIR"var/log/test.log"

[database]
host = localhost
dbname = test
user = root
password = test

[generate]
dir = ROOT_DIR"var/data/"
alias["test:home:([a-z]+)"] = "index-$1.html"
